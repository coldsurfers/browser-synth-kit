{
  "name": "demo",
  "private": true,
  "version": "0.0.0",
  "description": "Static browser playground for @coldsurf/synth-kit — pick an engine + preset, hit play.",
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @coldsurf/synth-kit build",
    "dev": "vite",
    "prebuild": "pnpm --filter @coldsurf/synth-kit build",
    "build": "vite build",
    "preview": "vite preview",
    "check:type": "tsc --noEmit",
    "predeploy": "pnpm run build",
    "deploy": "wrangler deploy",
    "precf:dev": "pnpm run build",
    "cf:dev": "wrangler dev"
  },
  "dependencies": {
    "@coldsurf/synth-kit": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^6.3.5",
    "wrangler": "^4.107.1"
  }
}
