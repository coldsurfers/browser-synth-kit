---
"@coldsurf/synth-kit": patch
---

Fix README quickstart to match the real API (`createWall` takes `initialTones`; drive time-varying parts via `rampGain`/`setChord`, not the non-existent `update()`/`fadeIn()`), and add a live demo link (synth-kit.coldsurf.io).
